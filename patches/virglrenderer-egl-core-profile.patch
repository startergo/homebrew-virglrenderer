--- a/src/vrend/vrend_winsys_egl.c	2026-02-03 12:39:08
+++ b/src/vrend/vrend_winsys_egl.c.new2	2026-02-03 12:39:47
@@ -707,10 +707,25 @@
       EGL_NONE
    };
 
+#if defined(__APPLE__)
+   /* macOS has no Compatibility Profile for OpenGL 3.2+.
+    * For Desktop GL contexts, force Core Profile regardless of compat_ctx.
+    * GLES (gl=es) passes through unchanged - it doesn't use compat_ctx. */
    if (vparams->compat_ctx) {
+      /* Guest requested compat profile, but macOS only has Core for OpenGL 3.2+
+       * Force Core Profile - the guest will adapt */
       ctx_att[4] = EGL_CONTEXT_OPENGL_PROFILE_MASK;
+      ctx_att[5] = EGL_CONTEXT_OPENGL_CORE_PROFILE_BIT;
+   }
+#else
+   if (vparams->compat_ctx) {
+      ctx_att[4] = EGL_CONTEXT_OPENGL_PROFILE_MASK;
       ctx_att[5] = EGL_CONTEXT_OPENGL_COMPATIBILITY_PROFILE_BIT;
+   } else {
+      ctx_att[4] = EGL_CONTEXT_OPENGL_PROFILE_MASK;
+      ctx_att[5] = EGL_CONTEXT_OPENGL_CORE_PROFILE_BIT;
    }
+#endif
 
    egl_ctx = eglCreateContext(egl->egl_display,
                              egl->egl_conf,
